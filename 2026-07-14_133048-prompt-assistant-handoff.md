# 提示词小助手：任务清单与交接文档

**交接时间：** 2026-07-14 13:30  
**项目状态：** 仅完成需求讨论与初步技术评估，尚未创建代码仓库，未开始设计或开发。  
**交接目标：** 让后续产品、设计或开发人员无需回看原对话即可继续推进。

---

## 一、项目概述

计划开发一款**开源、本地运行的桌面提示词助手**，面向文生图、文生视频、图生图和图生视频场景。

产品不直接负责生成图片或视频，第一阶段主要负责：

- 管理、分类和组合提示词；
- 支持中英文输入与输出；
- 根据目标生成模型优化提示词；
- 对模糊描述进行联想、补全和专业化改写；
- 允许用户配置免费额度 API、OpenAI Compatible API 或本地模型；
- 保存、导入和导出独立的提示词库文件；
- 后续通过视觉模型分析图片并生成、匹配和归类提示词。

建议产品形态：**Windows 优先的本地桌面应用**。候选技术方案为 Tauri + Vue/React；若优先验证原型，可考虑 Python + PySide6。

---

## 二、已确认的原始需求

1. 针对不同图片、视频模型进行提示词优化。
2. 支持中文、英文以及中英混合输入。
3. 对模糊输入自动联想、优化和补全。
4. API 由用户自定义配置，软件不依赖自建中心服务器。
5. 将提示词按图片、视频及人物、分镜、运镜、风格、画质、服装、场景、动作、表情、灯光、机位、构图、道具、滤镜、特效等维度分类。
6. 软件开源、本地运行，不要求远程更新。
7. 用户自定义提示词必须与软件内置提示词明确区分。
8. 图片分析生成提示词后，与本地词库进行语义匹配；找不到合适词条时生成新候选并归类。
9. AI 生成词条必须与内置和用户词条有不同的颜色、图标或文字标记。
10. 提示词解释通过系统浏览器打开 Bing 搜索结果。
11. 提示词以独立文件保存，规定正式格式，并支持导入、导出。

---

## 三、已形成的方案共识

### 1. 数据来源必须分开

每条词条使用 `source` 标记来源：

- `builtin`：软件内置，只读；
- `user`：用户创建，可编辑；
- `ai_generated`：AI 生成，默认待确认。

界面不能只靠颜色区分，还应显示图标或文字，避免无障碍问题。

### 2. 提示词与生成参数分开

应区分：

- 提示词文本；
- 模型生成参数，例如宽高比和分辨率；
- 软件内部元数据，例如分类、标签、来源和版本。

### 3. API 采用适配层

第一版至少支持：

- OpenAI Compatible API；
- 自定义 Base URL；
- 自定义 API Key；
- 自定义文本模型和视觉模型名称；
- 接口连通性测试；
- 本地 Ollama/LM Studio 等兼容服务。

软件可以免费开源，但不能承诺第三方 API 永久免费。

### 4. 图片“反推”应准确命名

建议使用“图片分析并生成提示词”或“图片转提示词”，不要承诺恢复原作者的真实提示词。

AI 新词条不应静默写入正式词库，应先进入“待确认”状态，由用户确认、修改、合并或拒绝。

### 5. 主数据格式采用 JSON

建议将内置、用户、AI 生成的数据分别保存，避免安装或升级时互相覆盖。

示例目录：

```text
prompt-library/
├── manifest.json
├── image/
│   ├── character.json
│   ├── clothing.json
│   ├── lighting.json
│   └── composition.json
├── video/
│   ├── storyboard.json
│   ├── camera-movement.json
│   └── transitions.json
├── user/
└── generated/
```

单条词条建议结构：

```json
{
  "id": "lighting_soft_cinematic_001",
  "type": "fragment",
  "mediaType": ["image", "video"],
  "category": "lighting",
  "name": {
    "zh-CN": "柔和电影灯光",
    "en": "soft cinematic lighting"
  },
  "prompt": {
    "zh-CN": "柔和的电影感灯光",
    "en": "soft cinematic lighting"
  },
  "description": {
    "zh-CN": "降低阴影反差，营造柔和的电影氛围",
    "en": "Creates a soft cinematic atmosphere with reduced shadow contrast"
  },
  "tags": ["柔光", "电影感", "人像"],
  "supportedModels": ["generic", "flux", "sdxl"],
  "source": "builtin",
  "status": "approved",
  "version": 1
}
```

---

# 四、任务清单

## P0：开发前必须确认

- [ ] **T01 确认首发平台**：仅 Windows，还是同步考虑 macOS/Linux。
- [ ] **T02 确认实现技术栈**：正式产品优先考虑 Tauri + Vue/React；快速原型可选 PySide6。
- [ ] **T03 确认第一版目标模型**：图片和视频各选择 2–3 个代表模型，避免首版支持范围失控。
- [ ] **T04 确认第一版是否只生成提示词**：建议暂不接入图片/视频生成接口。
- [ ] **T05 确认 AI 词条审核流程**：建议必须由用户确认后才进入正式词库。
- [ ] **T06 确认开源许可证**：MIT 或 Apache-2.0，并单独确认内置词库及示例素材许可。
- [ ] **T07 确认交互主模式**：以“卡片组合器”为主，还是以“聊天式优化”为主。
- [ ] **T08 确认第一版验收范围和发布时间**。

## P1：产品与数据设计

- [ ] **T09 编写 PRD**：补全目标用户、使用场景、功能边界、非目标和验收标准。
- [ ] **T10 设计信息架构**：图片、视频、通用三级入口及完整分类树。
- [ ] **T11 定义 JSON Schema v1**：覆盖词条、模板、模型规则、数据包清单及格式版本。
- [ ] **T12 设计重复与冲突规则**：ID 冲突、文本重复、语义重复、分类不存在、字段缺失。
- [ ] **T13 设计数据迁移机制**：保证未来格式升级时可迁移旧词库。
- [ ] **T14 建立摄影与生成模型术语表**：确保中英文翻译一致，避免机械直译。
- [ ] **T15 设计来源与状态系统**：`builtin/user/ai_generated` 与 `approved/pending/rejected`。
- [ ] **T16 设计模型规则文件**：定义语言偏好、提示词风格、字段顺序、负面提示词和参数能力。
- [ ] **T17 绘制低保真界面**：分类树、词条卡片、提示词工作区、API 设置、导入导出页面。
- [ ] **T18 确定本地存储方案**：JSON 文件为交换格式，应用索引可选 SQLite。

## P2：MVP 工程实现

- [ ] **T19 初始化代码仓库**：建立许可证、README、贡献指南、目录结构及基础 CI。
- [ ] **T20 建立桌面应用骨架**：窗口、路由、主题、错误边界和日志基础设施。
- [ ] **T21 实现词库加载与校验**：按 JSON Schema 读取内置、用户和 AI 数据。
- [ ] **T22 实现分类树与词条列表**：支持分类浏览、搜索、筛选和分页/虚拟滚动。
- [ ] **T23 实现词条编辑**：新增、编辑、删除用户词条，内置词条保持只读。
- [ ] **T24 实现提示词组合器**：添加、排序、移除片段，生成中文、英文或双语结果。
- [ ] **T25 实现来源标记**：颜色、图标、文字标签和无障碍说明。
- [ ] **T26 实现本地搜索**：名称、正文、标签、分类和模型筛选。
- [ ] **T27 实现 API 配置页**：Base URL、Key、模型名、请求参数及连通性测试。
- [ ] **T28 安全保存 API Key**：优先使用系统安全存储，日志中禁止输出完整 Key。
- [ ] **T29 实现模糊输入补全**：轻度补全、创意增强、专业优化三种模式。
- [ ] **T30 实现优化差异展示**：标记 AI 新增、删除和改写内容，并允许恢复原文。
- [ ] **T31 实现模型适配**：先做通用规则，再为首批目标模型增加独立规则文件。
- [ ] **T32 实现中英文转换**：术语表优先，复杂语句通过用户配置的 API 处理。
- [ ] **T33 实现导入导出**：首版至少支持单条 JSON、完整 JSON 包和纯文本。
- [ ] **T34 实现 Bing 搜索跳转**：对查询词 URL 编码后使用系统默认浏览器打开。
- [ ] **T35 建立示例内置词库**：使用具有明确许可、已去重并人工校验的数据。
- [ ] **T36 完成 MVP 测试**：单元、Schema、导入冲突、API 错误、桌面端关键流程测试。
- [ ] **T37 打包 Windows 安装包**：在干净 Windows 环境中验证安装、启动、卸载和数据保留。
- [ ] **T38 完成 MVP 验收**：依据下方验收标准逐项实测并记录结果。

## P3：图片分析与语义匹配

- [ ] **T39 实现图片导入**：文件选择、拖放、类型/大小校验和本地预览。
- [ ] **T40 增加第三方上传告知**：调用在线视觉 API 前明确提示隐私风险。
- [ ] **T41 定义视觉分析 JSON 输出**：人物、服装、场景、动作、构图、灯光、机位、风格等字段。
- [ ] **T42 实现视觉模型适配器**：独立配置视觉 API 和模型。
- [ ] **T43 选择本地多语言嵌入模型**：验证中英文同义词匹配效果与包体积。
- [ ] **T44 实现向量索引**：按分类过滤后计算相似度，保存至 SQLite 或本地索引。
- [ ] **T45 设计相似度策略**：向量相似度、标签、分类和语言结果加权。
- [ ] **T46 实现匹配结果页**：展示已有词条、相似度和匹配理由。
- [ ] **T47 实现 AI 候选词条流程**：未匹配内容进入待确认区，而非直接污染正式词库。
- [ ] **T48 实现合并与去重**：支持接受、编辑、合并、拒绝和撤销。
- [ ] **T49 建立图片分析评测集**：覆盖人物、风景、产品、动漫及复杂场景。

## P4：专业视频能力与后续扩展

- [ ] **T50 视频提示词结构化编辑**：分离主体运动、镜头运动、时间变化与转场。
- [ ] **T51 分镜编辑器**：多镜头卡片、镜头顺序和单镜头提示词。
- [ ] **T52 图生视频模板**：明确保留内容、变化内容、运动内容和一致性要求。
- [ ] **T53 首尾帧与时间轴**：支持首帧、尾帧、时长和阶段动作描述。
- [ ] **T54 插件式模型规则**：允许用户导入、编辑和导出模型适配文件。
- [ ] **T55 历史记录与撤销**：保存优化历史、词条版本和用户操作恢复点。
- [ ] **T56 离线模型支持增强**：为 Ollama/LM Studio 提供预设与配置帮助。

---

## 五、MVP 建议范围

首版只做以下闭环：

1. 本地分类提示词库；
2. 中英文词条；
3. 搜索、筛选、组合、复制；
4. 内置、用户、AI 来源区分；
5. JSON 导入导出；
6. OpenAI Compatible API 配置；
7. 模糊输入补全和提示词优化；
8. 少量代表性图片、视频模型规则；
9. 系统浏览器打开 Bing 搜索。

首版暂缓：

- 图片分析与语义匹配；
- 分镜时间轴；
- 批量图片处理；
- 云同步、登录和用户系统；
- 直接生成图片或视频；
- 覆盖所有生成模型。

---

## 六、MVP 验收标准

- [ ] 无网络时仍可浏览、搜索、组合、编辑和导入导出本地提示词。
- [ ] 用户可以配置任意 OpenAI Compatible Base URL、Key 和模型名称。
- [ ] API 不可用时给出明确错误，不丢失用户原始输入。
- [ ] 中文、英文及混合输入可以生成中文、英文或双语结果。
- [ ] 用户能看到 AI 对原文增加、删除或修改的内容。
- [ ] 内置词条不可被直接覆盖，用户可复制后自定义。
- [ ] 三种来源在颜色、图标和文字上均可区分。
- [ ] 导出的完整词库可在全新安装环境中重新导入。
- [ ] 导入过程可处理 ID 冲突、重复内容、缺失字段和不支持的格式版本。
- [ ] API Key 不出现在日志、导出包或错误详情中。
- [ ] 点击解释按钮会使用默认浏览器打开正确编码的 Bing 搜索地址。
- [ ] Windows 安装包能在干净环境中完成安装、运行和卸载。

---

## 七、主要风险与处理建议

| 风险 | 影响 | 建议 |
|---|---|---|
| 项目范围过大 | 首版长期无法完成 | 首批只支持少量模型，图片分析放到第二阶段 |
| 免费 API 不稳定 | 核心 AI 功能不可用 | 用户自配接口，同时支持本地兼容服务 |
| 模型规则快速过时 | 优化效果下降 | 规则使用独立 JSON 文件，支持手动导入 |
| 内置词库质量差 | 搜索和组合体验差 | 少而精、人工审核、去重、注明许可 |
| AI 自动生成污染词库 | 重复和错误持续累积 | 新词条进入待确认区，禁止静默正式入库 |
| 图片上传隐私 | 用户敏感内容泄露 | 调用第三方 API 前提示；支持本地视觉模型 |
| 只靠颜色区分来源 | 无障碍体验差 | 同时使用图标和文字标签 |
| “图片反推”承诺过度 | 用户预期错误 | 命名为“图片转提示词”，说明仅分析可见语义 |

---

## 八、尚未决定的问题

后续负责人开始开发前，需要产品决策者回答：

1. 第一版是否仅支持 Windows？
2. 选择 Tauri 还是 PySide6？
3. 第一版具体适配哪些图片和视频模型？
4. 主界面以卡片组合还是聊天优化为核心？
5. AI 生成词条是否一律需要确认？
6. 是否需要收藏、历史记录和撤销进入 MVP？
7. 内置词库由谁整理，其内容来源和许可证是什么？
8. 开源许可证选 MIT 还是 Apache-2.0？
9. 是否完全不接入图片/视频生成接口？
10. “biying”是否明确指 Bing（`https://www.bing.com/search?q=...`）？

---

## 九、当前交接状态

- **已有代码：** 无。
- **已有仓库：** 无；当前工作目录不是 Git 仓库。
- **已有设计稿：** 无。
- **已有正式 PRD：** 无。
- **已有提示词数据：** 无。
- **已有 API 或账号配置：** 无。
- **已完成工作：** 需求理解、初步架构建议、数据格式草案、阶段划分、难度和风险评估。
- **推荐下一步：** 先完成 P0 的八项产品决策，再编写 PRD 和 JSON Schema；不要直接进入编码。

此文档仅用于任务拆分和交接，没有继续实施项目。